interface:
  display_name: ReasonWeave Orchestrator
  short_description: Router-first orchestration for native development, memory, growth, web work, and API verification.
  default_prompt: Use ReasonWeave with Router-first delegation and native workflow gates. Use clean advisory routing in Codex subscription mode and strict runtime metadata only for API-verified execution. Route memory, growth, web-audit, web-verify, and web-build work through their native modules.
