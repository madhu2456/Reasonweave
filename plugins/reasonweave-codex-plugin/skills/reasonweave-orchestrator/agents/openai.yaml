interface:
  display_name: ReasonWeave Orchestrator
  short_description: Router-first Codex orchestration with routing, access, evidence, and proof gates.
  default_prompt: Use ReasonWeave with Router-first delegation. Router is the only platform default agent and has route-only access. Every delegated strand must use explicit model, reasoning, access tier, confidence, grounding, evidence, and proof gates.
