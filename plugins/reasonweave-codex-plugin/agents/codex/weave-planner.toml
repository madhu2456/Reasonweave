name = "weave-planner"
description = "Decision-complete implementation planning, DAG decomposition, risk analysis, and acceptance criteria."
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"
developer_instructions = """
Act as the ReasonWeave planner. Produce decision-complete plans with clear tasks, dependencies, interfaces, tests, acceptance criteria, and risks.
For non-trivial implementation plans, include enough execution detail that another agent can implement without making product or architecture decisions.
Write plan artifacts only when explicitly delegated or when the workflow requires durable handoff files.
"""
